import {CcCustomDirectiveDirective} from './cc-custom-directive.directive';

describe('CcCustomDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CcCustomDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
